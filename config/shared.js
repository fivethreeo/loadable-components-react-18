const clone = require("./clone")

const config = {
    moduleids: "deterministic",
    extensions: [".tsx", ".ts", ".jsx", ".js", ".json"],
    prodMode: "production",
    devMode: "development"
};

module.exports = config;